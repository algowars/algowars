import { Test, TestingModule } from '@nestjs/testing';
import { {{ properCase name }}Controller } from './{{ name}}.controller';
import { QueryBus } from '@nestjs/cqrs';

describe('{{ properCase name }}Controller', () => {
  let controller: {{ properCase name }}Controller;
  let queryBus: QueryBus;

  beforeEach(async () => {
    const module: TestingModule = await Test.createTestingModule({
      controllers: [{{ properCase name }}Controller],
      providers: [
        {
          provide: QueryBus,
          useValue: {
            execute: jest.fn(),
          },
        },
      ],
    }).compile();

    controller = module.get<{{ properCase name }}Controller>({{ properCase name }}Controller);
    queryBus = module.get<QueryBus>(QueryBus);
  });

  it('should be defined', () => {
    expect(controller).toBeDefined();
  });
});
