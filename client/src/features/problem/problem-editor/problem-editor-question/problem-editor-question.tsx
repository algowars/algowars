import { Problem } from "../../models/problem.model";

type Props = {
  problem: Problem;
};

const ProblemEditorQuestion = ({ problem }: Props) => {
  return <div>ProblemEditorQuestion</div>;
};

export default ProblemEditorQuestion;
