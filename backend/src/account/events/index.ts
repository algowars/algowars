import { AccountCreatedHandler } from './account-created.handler';

// AccountEventHandlers is an array that holds all event handlers related to account events.
// This array can be used to easily register all account event handlers in a module.
export const AccountEventHandlers = [AccountCreatedHandler];
