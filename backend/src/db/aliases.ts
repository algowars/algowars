export enum Aliases {
  ACCOUNTS = 'accounts',
  PROBLEMS = 'problems',
  LANGUAGES = 'languages',
  ADDITIONAL_TEST_FILES = 'additional_test_files',
  TESTS = 'tests',
  SUBMISSIONS = 'submissions',
  SUBMISSION_RESULTS = 'submission_results',
  PROBLEM_SETUPS = 'problem_setups',
  TAGS = 'tags',
  PROBLEM_TAGS = 'problem_tags',
  TEST_CASES = 'test_cases',
}
